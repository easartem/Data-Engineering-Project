package app

import java.util.{Timer, TimerTask}

object TaskScheduler {
  /**

  val taskEvent = new TimerTask {
    override def run(): Unit = {
      val rdd = generateEvent().first()
      handleEvent(rdd.score)
      //sendAlertRecord(rdd)
    }
  }

  val timerEvent = new Timer()
  timerEvent.schedule(taskEvent, 0, 6000)


  val taskReport = new TimerTask {
    override def run(): Unit = {
      val rdd = generateEvent().first()
      handleEvent(rdd.score)
      //sendAlertRecord(rdd)
    }
  }
  val timerReport = new Timer()
  timerReport.schedule(taskReport, 0, 6000)
  **/

}
